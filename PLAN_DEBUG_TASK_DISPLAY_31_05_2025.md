# План отладки проблемы с отображением задач на 31.05.2025

Дата: 2025-05-31

## 1. Отчет по текущей диагностике:

### 1.1. API `GET /tasks`:
*   **Результат**: API (`http://localhost:3001/tasks`) корректно возвращает массив задач. В этом массиве присутствуют задачи с `dueDate` в районе конца мая / начала июня 2025, включая задачу на "2025-05-31".
*   **Пример задачи от пользователя**:
    ```json
    {
        "uuid": "0ff33cc7-5d22-419e-919e-8208e194b8b2",
        "type": "income",
        "title": "Помощь по дому",
        "dueDate": "2025-05-31",
        "time": "10:00",
        "childId": "14cab86c-8319-4e52-98d9-9a988aa47eaa"
    }
    ```

### 1.2. Состояние `tasksForWeek` на фронтенде (в [`front/src/components/WeekView.tsx`](front/src/components/WeekView.tsx:1)):
*   **Результат**: После добавления `console.log` в [`front/src/components/WeekView.tsx`](front/src/components/WeekView.tsx:1), консоль браузера показывает, что массив `tasksForWeek` содержит все задачи, которые вернул API, включая задачу с `dueDate` "2025-05-31".
*   **Вывод**: Данные из API успешно загружаются и устанавливаются в основное состояние `tasksForWeek`.

### 1.3. Логика фильтрации задач для конкретных дней:
*   **Текущий статус**: Фильтрация происходит в компонентах [`front/src/components/FirstHalfOfWeek.tsx`](front/src/components/FirstHalfOfWeek.tsx:1) и [`front/src/components/SecondHalfOfWeek.tsx`](front/src/components/SecondHalfOfWeek.tsx:1) (строки 21-23 в [`SecondHalfOfWeek.tsx`](front/src/components/SecondHalfOfWeek.tsx:21-23): `tasksForWeek.filter(task => moment(task.dueDate).isSame(dayMoment, 'day'))`).
*   **Следующий шаг диагностики**: Добавить `console.log` после фильтрации в этих компонентах (начнем с [`SecondHalfOfWeek.tsx`](front/src/components/SecondHalfOfWeek.tsx:1)).

## 2. Предварительный вывод о причине проблемы:

Наиболее вероятная причина проблемы — **ошибка в логике фильтрации задач по дням** в компонентах [`front/src/components/FirstHalfOfWeek.tsx`](front/src/components/FirstHalfOfWeek.tsx:1) и/или [`front/src/components/SecondHalfOfWeek.tsx`](front/src/components/SecondHalfOfWeek.tsx:1). Возможно, сравнение дат `moment(task.dueDate).isSame(dayMoment, 'day')` не работает так, как ожидается.

## 3. План дальнейших действий:

1.  **Переключиться в режим "💻 Code"**.
2.  **Внести изменения для логирования в режиме "💻 Code"**:
    *   В файле [`front/src/components/SecondHalfOfWeek.tsx`](front/src/components/SecondHalfOfWeek.tsx:1):
        *   После строки 23 (`);`) добавить:
            ```typescript
            console.log('SecondHalf - Filtering for day:', dayMoment.format('YYYY-MM-DD'), 'Input tasksForWeek count:', tasksForWeek.length, 'Tasks found for this day:', tasksForDay);
            ```
    *   *Отложено (при необходимости)*: Аналогично внести изменения в [`front/src/components/FirstHalfOfWeek.tsx`](front/src/components/FirstHalfOfWeek.tsx:1).
3.  **Проанализировать вывод консоли**:
    *   Пользователь после внесения изменений перезагрузит страницу, перейдет на проблемную неделю и предоставит вывод из консоли браузера.
    *   Обратить особое внимание на дату `dayMoment`, количество задач в `tasksForWeek` и содержимое `tasksForDay` (особенно для 31.05.2025).
4.  **Выявить точную причину ошибки фильтрации**:
    *   Если `tasksForDay` пусто для 31.05.2025, детально рассмотреть значения `task.dueDate` и `dayMoment` в момент сравнения.
5.  **Исправить ошибку**:
    *   Скорректировать логику сравнения дат в `filter`.
6.  **Проверить результат**:
    *   Убедиться, что задачи корректно отображаются.
7.  **Удалить добавленные `console.log`**.

## 4. Визуализация плана (Mermaid):

```mermaid
graph TD
    A[Начало: Задачи не отображаются для 31.05] --> B{API возвращает задачи?};
    B -- Да --> C{tasksForWeek в WeekView содержит задачи?};
    C -- Да --> D{Проблема в фильтрации по дням?};
    D -- Да --> E[Переключиться в режим "Code"];
    E --> F[Добавить console.log в SecondHalfOfWeek.tsx];
    F --> G[Пользователь проверяет консоль];
    G --> H{Логи показывают ошибку фильтрации?};
    H -- Да --> I[Детальный анализ фильтра moment(task.dueDate).isSame(dayMoment, 'day')];
    I --> J[Исправить логику сравнения дат];
    J --> K[Проверить отображение задач];
    K -- Успешно --> L[Удалить console.log];
    L --> M[Конец: Задачи отображаются];
    H -- Нет (маловероятно) --> N[Пересмотреть гипотезу];

    subgraph "Этап 1: Диагностика (уже частично выполнена)"
        B
        C
    end

    subgraph "Этап 2: Углубленная диагностика и Исправление (в режиме Code)"
        E
        F
        G
        H
        I
        J
        K
        L
    end